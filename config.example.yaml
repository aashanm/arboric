# Arboric Configuration Example
# Copy this file to ~/.arboric/config.yaml and customize as needed

# Optimization algorithm settings
optimization:
  # Cost weight (0-1): Higher = prioritize cost savings
  cost_weight: 0.7

  # Carbon weight (0-1): Higher = prioritize carbon reduction
  # Note: cost_weight + carbon_weight must equal 1.0
  carbon_weight: 0.3

  # Minimum delay before starting workloads (hours)
  # Useful if you need setup/preparation time
  min_delay_hours: 0.0

  # Prefer continuous execution windows over fragmented ones
  prefer_continuous: true

# Default workload settings
# These values are used when options are not specified in CLI commands
defaults:
  # Default workload duration in hours
  duration_hours: 4.0

  # Default power draw in kilowatts
  power_draw_kw: 50.0

  # Default deadline in hours (must be >= duration)
  deadline_hours: 12.0

  # Default grid region
  # Options: US-WEST, US-EAST, EU-WEST, NORDIC
  region: US-WEST

# Live grid data settings (optional)
# By default, Arboric uses realistic MockGrid simulation
# For live emissions data, install: pip install arboric[cloud]
# Then configure your provider credentials below
live_data:
  enabled: false
  provider: "YOUR_EMISSIONS_PROVIDER"
  api_key: "YOUR_PROVIDER_API_KEY"
  api_secret: "YOUR_PROVIDER_API_SECRET"

# CLI display and behavior settings
cli:
  # Show ASCII banner on startup
  show_banner: true

  # Color theme for terminal output
  # Options: default, minimal, mono
  color_theme: default

  # Minimize output (overrides show_banner)
  quiet_mode: false

  # Skip confirmation prompts (use with caution)
  auto_approve: false
